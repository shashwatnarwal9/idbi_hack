# आय·AI (AayAI) — dependencies. Later stages append their own (great_expectations,
# xgboost, scikit-learn, shap, apache-airflow, psycopg2, streamlit, ...).
duckdb>=1.1
pytest>=8.0
great_expectations>=1.3
pandas>=2.0
# xgboost pinned <3: shap's tree loader (0.49 in the Airflow image, capped by
# its numpy 1.26) cannot parse the vectorised base_score of xgboost >=3.1 models
xgboost>=2.1,<3
scikit-learn>=1.5
shap>=0.46
psycopg2-binary>=2.9
streamlit>=1.35
